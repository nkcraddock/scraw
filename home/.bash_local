function setup-github() {
  ssh-keygen -f ~/.ssh/id_rsa -t rsa -N ''
  KEY=`cat ~/.ssh/id_rsa.pub`
  URL='https://api.github.com/user/keys'
  HOSTNAME=`hostname`
  DATA="{\"title\":\"docker@$HOSTNAME\", \"key\": \"$KEY\"}"
  curl --user "$1" --data "$DATA" $URL
}
cd ~/dev/

